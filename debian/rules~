#!/usr/bin/make -f
#%:
#	dh $@

#override_dh_auto_install:
#	        $(MAKE) DESTDIR=$$(pwd)/debian/chaudhary prefix=/usr install

LDFLAGS=$(shell echo $$LDFLAGS | sed -e 's/-Wl,-Bsymbolic-functions//')
install:
	cp chaudhary.sh usr/bin/chaudharybla
clean:
	#rm chaudharybla
binary-install:
	#
build:
	#
